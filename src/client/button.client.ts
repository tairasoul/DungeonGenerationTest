/*import iris from "@rbxts/iris"
import { remotes } from "shared/remotes";

iris.Init();

iris.Connect(() => {
    iris.Window(["debug window"]);
    if (iris.Button(["re-apply offsets"]).clicked()) {
        print("re-applying offsets")
        remotes.async.fire();
    }
    iris.End();
});*/