-- Compiled with roblox-ts v2.3.0
local Workspace = game:GetService("Workspace")
local tileFolder = Instance.new("Folder", Workspace)
tileFolder.Name = "Dungeons"
local dungeonFolder = tileFolder
return {
	dungeonFolder = dungeonFolder,
}
